program TEST

var a,b,c,d: bool;
var x,y : int;
begin
(*a := true or (c and (x < y));*)
(*c := ((b or a) = (c and a));*)
(*a := ( a and c ) or ( d and b);*)
(*a := ( a or b ) and not ( a and b);*)
a := a <> b;
end
